{% extends 'layout/base.twig' %}

{% block title %}
    {{ produto.titulo }}
{% endblock %}

{% block content %}
        <h1 class="text-4xl font-bold mb-4">{{ produto.titulo }}</h1>
        <p class="text-xl mb-2">Preço: R$ {{ produto.preco|number_format(2, ',', '.') }}</p>
        <p class="mb-4">{{ produto.descricao }}</p>
        <a href="/"><button class="mt-4 px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">Voltar para a Home</button></a>
{% endblock %}
